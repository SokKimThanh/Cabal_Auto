This file previously contained an ad-hoc patch snippet for
tests/unit/test_target_bar_detector.py.

The corresponding test change should live in the test file itself rather than
as a standalone unified diff artifact in the repository.

This note is kept only to avoid retaining misleading patch metadata here.
