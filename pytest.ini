[pytest]
addopts = --import-mode=importlib
markers =
    fast: Unit tests nhanh không chạm I/O
    db: Tests liên quan đến SQLite in-memory
    ui: Tests logic giao diện (đã mock)
    slow: Integration tests chạy sâu
    manual: Test cần tương tác thủ công, không chạy trong CI
