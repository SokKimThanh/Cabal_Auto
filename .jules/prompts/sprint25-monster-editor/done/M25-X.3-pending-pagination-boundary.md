# M25-X.3 - Pending Pagination Boundary

Paste `00-global-rules.md` before this prompt.

```text
Refactor/hardening session, 20-25 minutes. Run only after M25-5 and M25-6 have passed.

Files in scope:
- ui/windows/monster_manager_win.py
- focused tests for pending_changes, filtering, pagination, refresh, and failed persistence

Goal:
Prove pending data is independent of the current DB page and remains recoverable until persistence succeeds.

Tasks:
- Verify pending_changes merge is keyed by monster ID and never depends on current page order/index.
- Verify add/edit pending records survive refresh, filter change, next/previous page, and dialog close/reopen.
- Verify a pending record excluded by the active filter/page remains stored and is reported as pending rather than discarded.
- Verify pending_changes clears only after every persistence operation succeeds; simulated DB/JSON failure retains all pending records and gives an actionable error.

Do not:
- Change MonsterEditDialog widgets/field metadata, database schema, JSON format, filter query semantics, or replace the full dataset with the current page.

Acceptance:
- Pending records survive all listed navigation operations.
- Duplicate-name validation queries/compares the complete relevant dataset, not just visible table rows.
- Failed persistence leaves pending data intact; successful persistence clears it exactly once.

Validation:
- Add/run focused tests for add/edit + refresh, filter, multi-page navigation, duplicate on another page, failed persistence, and successful retry.
- Run the narrowest manager/import smoke check.
- Report all boundary cases and whether any recovery/rollback was required.
```