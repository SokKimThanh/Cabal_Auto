# M25-X.1 - Reference And Null Guard

Paste `00-global-rules.md` before this prompt.

```text
Refactor/hardening session, 20-25 minutes. Run only after M25-4 and M25-6 have passed.

Files in scope:
- dialogs/monster_edit.py
- focused tests for reference mapping and nullable values

Goal:
Harden the reference-field boundary without changing form ownership, database schema, or persistence format.

Tasks:
- Verify Combobox display labels map only to canonical dungeonId/serverBossType IDs/values in the candidate record.
- Preserve a historical reference ID that no longer exists in the current database reference list. Show it as an explicit unavailable/legacy option; do not crash, silently replace it, or coerce it to None.
- Ensure empty selection maps to Python None and None never becomes the string "None" in candidate data, DB writes, or JSON fallback.

Do not:
- Alter get_dungeon_list/get_monster_type_list semantics, seed reference tables, modify manager pagination/pending state, or add a new source of truth for reference data.

Acceptance:
- Label-to-ID mapping emits the canonical FK value.
- Missing historical reference survives Edit -> Collect unchanged.
- Empty reference remains None through Edit -> Collect.

Validation:
- Add/run focused tests for populated, empty, and historical-missing reference cases.
- Run the narrowest dialog/import smoke check.
- Report source of reference options, candidate values, and all three cases as passed, failed, or manual-only.
```