🧩 Ngữ cảnh dự án: Auto Game Cabal Origin bằng Python (Cập nhật 2025-10-18, Hunt GUI & Monster Templates Sprint 2)

🎯 Mục tiêu
- Auto thao tác an toàn cho Cabal VTC Origin: ưu tiên gửi phím (không cần click), mở rộng nhận diện ảnh đa mẫu.
- Cung cấp GUI Hunt ổn định: chọn đúng cửa sổ game theo PID/HWND, đưa lên trước, thu nhỏ GUI để phím vào game, có hotkey dừng toàn cục.
- Thiết kế lại quản lý quái/thư viện ảnh: CRUD tách riêng, hỗ trợ nhiều template 2D mỗi quái và lưu vùng cửa sổ.

🛠️ Môi trường hiện tại
- Hệ điều hành: Windows
- Thư mục project: E:\Cabal_Auto
- Python: 3.14.0 (venv tại `E:\Cabal_Auto\venv`)
- Trình chạy: PowerShell (khuyến nghị Start VS Code/PowerShell As Administrator để global hotkey đáng tin cậy hơn)

📦 Thư viện trong venv
- pyautogui, keyboard, pillow
- Khuyến nghị thêm (tùy chọn): pygetwindow, psutil
- Lưu ý: opencv-python (cv2) có thể chưa tương thích Python 3.14. Nếu cần confidence cho template matching, dùng Python 3.10/3.11 rồi cài opencv-python.

📁 Cấu trúc thư mục tiêu biểu
E:\Cabal_Auto
├── assets/
│   ├── documents/Ngữ cảnh tạo auto cabal.txt
│   └── images/                       # Lưu ảnh template (ví dụ target_frame.png)
├── app_gui.py                        # GUI Hunt (chỉ còn tab Hunt)
├── auto_hunt.py                      # Script Hunt không GUI
├── hunt_config.json                  # Cấu hình Hunt
├── win_input.py                      # Gửi phím qua Windows SendInput (ctypes)
├── skills.json                       # Danh sách skill (key + cooldown)
├── monsters.json                     # Danh sách quái (schema mới: templates[], window_bounds)
├── main_skills.py                    # Tự gửi skill theo cooldown
├── main_safe.py                      # Auto click/keys an toàn (hotkey, failsafe)
├── config.json                       # Cấu hình click/hotkey cơ bản; có thể bổ sung UI
├── README.md                         # Hướng dẫn chi tiết
├── requirements.txt                  # Danh sách thư viện
└── venv/                             # Môi trường ảo

⚙️ Cấu hình chính
- config.json (clicker cơ bản)
	{
		"click": { "x": 500, "y": 400, "interval_sec": 2.0 },
		"hotkeys": { "toggle": "f8", "exit": "f9" },
		"safety": { "failsafe": true, "pause_key": "f7" }
	}

- hunt_config.json (Hunt trong GUI hoặc CLI)
	Các khóa điển hình: 
	- window_title: chuỗi cần có trong tên cửa sổ (ví dụ "CABAL")
	- window_pid (tùy chọn) và/hoặc window_hwnd (tùy chọn): ưu tiên focus chính xác
	- target_key: phím chuyển mục tiêu (thường "TAB")
	- attack_keys: mảng phím tấn công, ví dụ ["1", "2", "3"]
	- attack_press_ms: thời gian nhấn mỗi phím (ms)
	- target_cycle_delay: độ trễ giữa các lần nhấn target_key khi đang tìm (s)
	- search_interval, attack_interval: chu kỳ quét ảnh/đánh (s)
	- lost_timeout_sec: thời gian mất dấu mục tiêu trước khi quay lại tìm (s)
	- template_path: đường dẫn ảnh template (tạm dùng trước khi hoàn tất thư viện template đa ảnh)
	- region: [left, top, width, height] để giới hạn tìm kiếm (sẽ chuyển sang cơ chế mặc định theo biên cửa sổ)
	- confidence, grayscale: tham số cho OpenCV nếu có
	- window_bounds (định hướng mới): {left, top, width, height} lưu biên cửa sổ game để mặc định vùng tìm kiếm
	- bring_to_front_each_cycle: true/false, có đưa game lên trước mỗi vòng hay không

▶️ Cách chạy (PowerShell)
1) Kích hoạt venv và cài thư viện (nếu chưa có)
	 - E:\Cabal_Auto\venv\Scripts\Activate.ps1
	 - pip install -r requirements.txt

2) Chạy GUI Hunt
	 - E:\Cabal_Auto\venv\Scripts\python.exe app_gui.py
	 Lưu ý: Chạy As Administrator giúp hotkey toàn cục (F9) ổn định hơn.

3) Chạy các script không GUI (tùy nhu cầu)
	 - E:\Cabal_Auto\venv\Scripts\python.exe main_safe.py
	 - E:\Cabal_Auto\venv\Scripts\python.exe main_skills.py
	 - E:\Cabal_Auto\venv\Scripts\python.exe auto_hunt.py

⌨️ Hotkey và thao tác quan trọng
- Trong GUI Hunt:
	- F9: dừng Hunt toàn cục (global hotkey, kể cả khi GUI bị thu nhỏ)
	- ESC: dừng Hunt trong GUI
	- GUI sẽ tự thu nhỏ (minimize) sau khi focus game thành công để phím đi vào game, không vào GUI
- Trong clicker an toàn (main_safe.py):
	- F8: bật/tắt auto
	- F7: tạm dừng/tiếp tục
	- F9: thoát chương trình
	- Failsafe: đưa chuột lên góc trên-trái (0,0) để ngắt khẩn cấp

🧠 Hành vi Hunt thông minh (state machine)
- Chế độ tìm (search): chỉ gửi target_key theo nhịp target_cycle_delay cho đến khi ảnh template xuất hiện trong region.
- Khi phát hiện mục tiêu: chuyển sang chế độ đánh (attack), lặp các attack_keys với attack_interval.
- Nếu mất ảnh mục tiêu liên tục quá lost_timeout_sec: quay lại chế độ tìm, tránh đổi mục tiêu quá sớm.
- Có thể cấu hình đưa cửa sổ game lên trước mỗi vòng để tăng độ tin cậy nhập phím.

� Quản lý kỹ năng (cập nhật 2025-10-17)
- attack_keys trong hunt_config sẽ được sinh tự động từ danh sách skill_slots; textbox hiện tại chuyển thành read-only, chỉ mở chế độ chỉnh tay (advanced override) khi thực sự cần.
- Schema skill mở rộng: giữ `cooldown` và `cast_time`, bổ sung `duration_sec` (buff uptime), `pre_refresh_sec` (thời điểm recast trước khi hết buff) và `hold_ms` (ép thời gian giữ phím khi khác cast_time); runtime sẽ tách lane attack/buff để áp lịch chính xác.
- Khi người dùng chọn ảnh kỹ năng, chương trình sao chép về `assets/images/skills/<slug>.png` (slug từ tên + timestamp) và lưu đường dẫn tương đối trong skills.json để tránh liên kết tới thư mục cá nhân.
- Nếu người dùng thay ảnh, xóa bản sao cũ trong thư mục dự án nhưng tuyệt đối không xóa file gốc của họ.
- Khi migrate dữ liệu cũ, đặt mặc định `duration_sec = 0`, `pre_refresh_sec = 0` và bỏ qua `hold_ms` để không phá vỡ skill đang có.

�🪟 Focus đúng cửa sổ game (PID/HWND)
- GUI liệt kê danh sách cửa sổ (lọc theo từ khóa), kèm PID và (tùy) tên process.
- Ưu tiên focus theo HWND hoặc PID (chính xác hơn tiêu đề). Sau khi focus thành công, GUI sẽ minimize.
- Có nút Bring To Front để thử đưa game lên foreground trước khi Start Hunt.

🌐 Ngôn ngữ và hiển thị
- GUI hỗ trợ i18n (EN/VI) và có trạng thái (status) theo thời gian thực khi đang Hunt.
- Tab Click đã được loại bỏ. Chỉ còn tab Hunt để đơn giản hóa và tránh xung đột phím (TAB) làm chuyển tab.

🧪 Chuẩn bị template và region
- Thư viện ảnh mới: mỗi quái lưu nhiều template (góc/camera khác nhau) với ngưỡng confidence riêng (~0.8).
- Template được quản lý trong `monster_templates/` (định hướng) và liên kết trực tiếp với quái trong `monsters.json`.
- Mặc định vùng tìm là toàn bộ cửa sổ game (lưu `window_bounds`); vẫn cho phép đặt region tùy quái để tối ưu tốc độ.
- Nếu dùng Python 3.10/3.11 + opencv-python: có thể đặt confidence/grayscale để nhận diện chắc hơn; hiện tại fallback pyautogui.

📑 Schema monsters.json (cập nhật 2025-10-18)
- Trường chính trên mỗi quái:
	- id (tùy chọn), name, description (tùy chọn)
	- window_bounds: { left, top, width, height } — biên cửa sổ game mặc định để tìm ảnh
	- templates: danh sách các mẫu ảnh, mỗi phần tử gồm:
		- name: tên mẫu (tùy chọn)
		- path: đường dẫn ảnh (ưu tiên tương đối trong dự án)
		- threshold: ngưỡng khớp (0..1), mặc định gợi ý ~0.80–0.90
		- region_strategy: "window" | "custom" (mặc định dùng biên cửa sổ; custom dùng region riêng)
		- region: [left, top, width, height] — chỉ có khi region_strategy = "custom"
		- grayscale (tùy chọn): true/false khi dùng OpenCV
- Tương thích ngược: nếu còn trường đơn `template_path` ở nơi khác, runtime sẽ lấy template đầu tiên trong `templates[]` làm mặc định khi cần.
- Ví dụ rút gọn:
	{
		"name": "Coc go~",
		"description": "Quái rừng mở đầu",
		"window_bounds": { "left": 100, "top": 100, "width": 1280, "height": 720 },
		"templates": [
			{
				"name": "goc-45",
				"path": "assets/images/monsters/coc-45.png",
				"threshold": 0.85,
				"region_strategy": "window"
			},
			{
				"name": "zoom-gan",
				"path": "assets/images/monsters/coc-zoom.png",
				"threshold": 0.9,
				"region_strategy": "custom",
				"region": [200, 150, 600, 300]
			}
		]
	}

🧩 Kế hoạch UI nhận diện quái (bắt đầu 2025-10-17)
- Sprint 1 (Hoàn thành 2025-10-17): dựng frame “Quản lý quái” riêng trong GUI (sidebar danh sách quái, form chi tiết). Cho phép CRUD cơ bản (thêm/sửa/xóa quái), nhập mô tả và lưu `window_bounds`.
- Sprint 2 (Hoàn thành 2025-10-18): thêm panel “Templates” trong form quái với CRUD mẫu: nhập tên, chọn ảnh, duyệt/browse, thiết lập `threshold`, `region_strategy` và `region` override. Đồng bộ lưu vào `monsters.json`, hiển thị preview đường dẫn và đảm bảo tương thích với cấu trúc cũ.
- Sprint 3: tích hợp preview trực quan (hiển thị `window_bounds` + overlay region). Ưu tiên logic đọc cấu hình mới → cập nhật `monsters.json` schema (`templates[]`, `window_bounds`, metadata khác).
- Sprint 4: kết nối Hunt tab với schema mới: selector quái lấy templates đa mẫu, runtime ưu tiên OpenCV (khi khả dụng) và fallback PyAutoGUI. Bổ sung logging kết quả nhận diện để debug.
- Sprint 5: polish UX (localization label, validate ngưỡng, tooltip hướng dẫn), tối ưu load ảnh (cache thumbnail) và chuẩn bị test regression cho vòng Hunt.

🛠️ Khắc phục sự cố nhanh
- Phím đi vào GUI thay vì game: dùng Bring To Front, chọn đúng PID/HWND, đảm bảo GUI đã minimize.
- Global hotkey không bắt được: chạy VS Code/PowerShell As Administrator.
- Tìm ảnh chậm hoặc sai: đặt region nhỏ, dùng template rõ ràng; cân nhắc dùng OpenCV trên Python 3.10/3.11.
- Game không nhận phím: anti-cheat có thể chặn. SendInput thường tốt hơn pyautogui. Chạy As Admin có thể giúp nhưng không đảm bảo vượt mọi hạn chế.

🧭 Hướng phát triển tiếp theo
- Đã xong:
	- Bước 1: mở rộng `monsters.json` để lưu `templates[]` và `window_bounds`.
	- Bước 2: hoàn tất cửa sổ “Quản lý quái” với panel Templates (CRUD mẫu, threshold, region override).
- Tiếp theo:
	- Bước 3: refactor vòng Hunt dùng danh sách `templates[]` (thử ưu tiên OpenCV trên Python 3.10/3.11, fallback PyAutoGUI). Duy trì tương thích `template_path` cũ như fallback.
	- Tích hợp preview overlay (Sprint 3): vẽ `window_bounds` và region của template trên ảnh/screenshot.
	- Triển khai workflow sao chép ảnh vào thư mục dự án (assets/images/monsters), lưu đường dẫn tương đối; dọn dẹp ảnh cũ an toàn khi thay thế.
	- Logging kết quả nhận diện (điểm khớp, template trúng) để debug; thêm lựa chọn không minimize GUI khi Start; expose `lost_timeout_sec` trong UI.
	- Kỹ năng: tiếp tục refactor editor + runtime theo schema mới, auto-copy ảnh, migration `skills.json`.

============================
🔄 Tổng kết cập nhật (2025-10-18)
============================
- Hoàn tất Sprint 2 quản lý quái: panel Templates với CRUD mẫu, threshold và region override; lưu `description`, `window_bounds` và `templates[]` vào `monsters.json`.
- Tương thích ngược: nơi còn dùng `template_path`, tạm thời lấy template đầu tiên từ `templates[]` làm mặc định.
- GUI Hunt: một tab, liệt kê PID/HWND, Bring To Front, tự minimize khi chạy; F9 dừng toàn cục, ESC dừng trong GUI; state machine search ↔ attack với `lost_timeout_sec`.
- Chuẩn bị Sprint 3: preview overlay, refactor runtime để thử nhiều template theo thứ tự, logging kết quả; cân nhắc bật OpenCV trên Python 3.10/3.11.
- Kỹ năng: workflow đã chốt (skill_slots → attack_keys, duration/pre-refresh/hold_ms, auto copy ảnh) — sẽ tiếp tục triển khai/migrate.