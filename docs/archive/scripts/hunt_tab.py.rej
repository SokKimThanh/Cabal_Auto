--- hunt_tab.py
+++ hunt_tab.py
@@ -306,7 +306,7 @@
         for col in range(num_slots):
             # Cột chứa label slot (Skill 1)
-            slot_frame.grid_columnconfigure(col*4, weight=0, minsize=60)
+            slot_frame.grid_columnconfigure(col * 4, weight=0, minsize=60)
             # Cột chứa Combobox - cho phép mở rộng
-            slot_frame.grid_columnconfigure(col*4 + 1, weight=1)
+            slot_frame.grid_columnconfigure(col * 4 + 1, weight=1)
             # Cột chứa Key label
-            slot_frame.grid_columnconfigure(col*4 + 2, weight=0)
+            slot_frame.grid_columnconfigure(col * 4 + 2, weight=0)
--- /dev/null
+++ /dev/null
@@ -317,14 +317,14 @@
             lbl.grid(row=row, column=col_base, sticky="e", padx=(4, 2))

             cmb = ttk.Combobox(
                 slot_frame, values=[], state="readonly", width=12
             )
-            cmb.grid(row=row, column=col_base+1, sticky="we", padx=(4, 0), pady=1)
+            cmb.grid(row=row, column=col_base + 1, sticky="we", padx=(4, 0), pady=1)
             self._skill_slot_combos.append(cmb)

             key_lbl = tk.Label(
                 slot_frame, text=self._t("hotkey"), bg=UI.BG_PANEL, font=UI.FONT_DEFAULT
             )
-            key_lbl.grid(row=row, column=col_base+2, padx=(2, 0))
+            key_lbl.grid(row=row, column=col_base + 2, padx=(2, 0))

             key_entry = tk.Entry(
